\documentclass[lualatex,paper=a4,fontsize=9pt]{jlreq}

\usepackage{amsmath,amssymb}
\usepackage{booktabs}
\usepackage{geometry}
\usepackage{graphicx}
\usepackage{longtable}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,fit,calc,backgrounds}

\geometry{margin=14mm}
\pagestyle{plain}
\setlength{\parindent}{0pt}
\setlength{\parskip}{0.55em}
\sloppy

\title{Telescope Array Low-energy Extension Surface Detector（TALE-SD）\\
波形グラフニューラルネットワークモデル仕様}
\author{}
\date{}

\newcommand{\code}[1]{\ifmmode\text{\ttfamily #1}\else\texttt{#1}\fi}
\newcommand{\R}{\mathbb{R}}

\begin{document}
\maketitle
\vspace{-7mm}

\section{目的と対象}

本書は、Telescope Array Low-energy Extension Surface Detector（TALE-SD、TALE 地表検出器）のモンテカルロ事象を対象とした、波形グラフニューラルネットワーク（Graph Neural Network: GNN）の現行仕様をまとめる。
対象は、幾何とエネルギーの再構成、および陽子・鉄の二値分類である。
本書の目的は、実装を逐一読まなくても、入力、グラフの作り方、ニューラルネットワークの層構成、出力、損失関数、学習設定を再現できるようにすることである。

現行の実行入口は次の 2 つである。
\begin{itemize}
  \item 再構成: \code{scripts/submit\_server\_waveform\_full\_training.sh}
  \item 質量分類のみ: \code{scripts/submit\_server\_mass\_only\_training.sh}
\end{itemize}
どちらのスクリプトも、Hierarchical Data Format version 5（HDF5）形式で保存済みのグラフ片を読み込む。
学習中に Data Summary Tape（DST）ファイルは読まない。
本書には、旧基準グラフでの比較結果と、2026 年 6 月 1 日に完了した旧 schema \code{flat50000} の結果を併記する。
旧基準比較で用いたグラフ入力は次である。
\begin{flushleft}
\small\code{/dicos\_ui\_home/ikomae/work/gnn/graphs/waveform\_gnn\_stream\_20260520\_123802}
\end{flushleft}
DST から作り直した energy-flat HDF5 は正常に完了している。
最大規模の energy-flat HDF5 は次である。
\begin{flushleft}
\small\code{/dicos\_ui\_home/ikomae/work/gnn/graphs/}\\
\small\code{server\_graph\_export\_energyflat200000\_20260524\_075508/}\\
\small\code{server\_graph\_export\_energyflat200000\_20260524\_075508.h5}
\end{flushleft}
ただし、この最大規模 HDF5 は最終確認用であり、通常のパラメータ確認には重い。

現行の波形 mask schema と 6 成分 target schema で作り直した、次の再学習用 HDF5 は次である。
\begin{flushleft}
\small\code{/dicos\_ui\_home/ikomae/work/gnn/graphs/}\\
\small\code{flat50000\_wfmask\_target6\_20260603\_102418/}\\
\small\code{flat50000\_wfmask\_target6\_20260603\_102418.h5}
\end{flushleft}
この HDF5 は 29 shard、2,839,184 事象、99.48 GiB であり、陽子 1,455,815 事象、鉄 1,383,369 事象、source path 43,032 個を含む。
旧 \code{flat50000} は波形表現と target schema が異なるため、現行モデルの再学習入力としては使わない。
1 回の学習に半日から 1 日程度を要するため、数十回から数百回の再学習を前提にした特徴量選択は行わない。

\section{実装とコードの読み方}

本節は、実際のコードを読むための地図である。
この PDF はモデル仕様を説明する文書だが、実装を追う場合には、DST から HDF5 グラフを作る段階と、HDF5 グラフを読んで学習する段階を明確に分けて読む必要がある。
全体の流れは次の通りである。
\[
\mathrm{DST}
\rightarrow
\code{talesd-gnn export}
\rightarrow
\mathrm{HDF5\ graph}
\rightarrow
\code{H5GraphDataset}
\rightarrow
\code{train\_model}
\rightarrow
\code{PhysicsTaleSdGNN}
\rightarrow
\mathrm{loss/metrics/diagnostics}.
\]

\begin{table}[htbp]
  \centering
  \caption{主要な処理段階と対応するソースコード。}
  \label{tab:implementation-map}
  {\small
  \begin{tabular}{p{30mm}p{58mm}p{72mm}}
    \toprule
    処理段階 & 主なファイル & ここで決まること \\
    \midrule
    CLI の入口 &
    \code{src/talesd\_gnn\_reconstruction/cli.py} &
    \code{talesd-gnn} の subcommand を解釈し、Python API の引数へ変換する。 \\
    DST 読み込み &
    \code{src/talesd\_gnn\_reconstruction/dst\_reader.py} &
    DST bank を順に読み、event graph 作成に渡す record を作る。 \\
    event graph 作成 &
    \code{src/talesd\_gnn\_reconstruction/event\_graph.py} &
    accepted-pulse node、edge、waveform、教師ラベル、metadata を作る。 \\
    HDF5 書き込み &
    \code{src/talesd\_gnn\_reconstruction/graph\_io.py} &
    graph shard を書き、\code{columns\_json} や \code{waveform\_schema} を保存する。 \\
    HDF5 読み込み &
    \code{src/talesd\_gnn\_reconstruction/dataset.py} &
    shard 群を 1 つの dataset として扱い、DataLoader 用の sample を返す。 \\
    学習全体 &
    \code{src/talesd\_gnn\_reconstruction/train.py} &
    split、scaler、DataLoader、学習 loop、loss、best checkpoint、最終評価を管理する。 \\
    モデル本体 &
    \code{src/talesd\_gnn\_reconstruction/model.py} &
    node、edge、waveform を埋め込み、message passing と graph readout で出力を作る。 \\
    診断図 &
    \code{src/talesd\_gnn\_reconstruction/diagnostics.py} &
    learning curve、エネルギー依存性、quality cut、mass 診断を作る。 \\
    入力分布・重要度 &
    \code{src/talesd\_gnn\_reconstruction/feature\_analysis.py} &
    入力特徴量分布と post-hoc feature group ablation を作る。 \\
    \bottomrule
  \end{tabular}
  }
\end{table}

\code{talesd-gnn} は重い処理を直接実装する本体ではなく、薄い command-line wrapper である。
\code{cli.py} の \code{\_cmd\_export}、\code{\_cmd\_train}、\code{\_cmd\_predict}、
\code{\_cmd\_input\_distributions}、\code{\_cmd\_feature\_importance} が、
それぞれ export、学習、推論、入力分布、特徴量重要度の実装関数を呼ぶ。
したがって、挙動を確認する時は、CLI 引数だけでなく、最終的に呼ばれる Python 関数の引数を見る。

学習時の中心は \code{train.train\_model} である。
この関数は、dataset 初期化、train/validation/test split、scaler fitting、DataLoader 作成、epoch loop、validation、best checkpoint 保存、test 評価、diagnostics 作成をまとめて実行する。
一方、\code{model.PhysicsTaleSdGNN} は forward pass のみを担当し、loss の計算は \code{train.py} 側に分離されている。
この分離により、同じ model 出力に対して、再構成 loss、quality loss、mass loss、energy bias penalty を組み合わせられる。

Slurm での本番学習では、通常は \code{talesd-gnn train} を直接打たず、\code{scripts/submit\_server\_*.sh} を使う。
これらの submitter は resource、run directory、local graph cache、runtime copy、環境変数を設定した後、最終的に \code{scripts/train\_large\_existing\_graphs.sh} から \code{talesd-gnn train} を呼ぶ。
\code{submit\_server\_reco\_mass\_training.sh} や \code{submit\_server\_mass\_only\_training.sh} は、task 固有の既定値を設定してから共通 submitter に渡す。
そのため run を比較する時は、script 名だけでなく、run directory に保存された設定、metrics JSON、diagnostics summary を確認する。

\section{用語と表記}

本書では、略語を初めて用いる箇所で正式名称を示す。
その後は、読みやすさを保つために略語を用いる。
広く使われている日本語の用語がある場合は、日本語を主に用い、必要に応じて括弧内に英語名と略語を示す。

本書で用いる主な機械学習用語は次の意味である。

\begin{itemize}
  \item 特徴量とは、モデルに入力する数値である。本解析では、検出器の位置、信号時刻、信号量、波形、検出器間の時刻差などが特徴量に相当する。
  \item 教師ラベルとは、教師あり学習でモデルに与える正解値である。再構成では真のエネルギー、コア位置、到来方向を用い、質量分類では陽子または鉄のラベルを用いる。
  \item 標準化とは、学習データの平均を引き、標準偏差で割る変換である。特徴量のスケールをそろえることで、最適化を安定させる。
  \item バッチとは、1 回のパラメータ更新に使う事象の集合である。エポックとは、学習データ全体を 1 回使う単位である。
  \item 損失関数とは、予測と正解のずれを数値化する関数である。学習では、この値が小さくなるようにモデルのパラメータを更新する。ただし、負の対数尤度を含む場合など、損失の最小値が 0 である必要はない。
  \item SmoothL1 損失とは、小さい誤差では二乗誤差、大きい誤差では絶対値誤差として振る舞う損失である。しきい値を $\beta$、誤差を $r$ とすると、
  \[
    \mathrm{SmoothL1}_{\beta}(r)=
    \begin{cases}
      r^2/(2\beta), & |r|<\beta,\\
      |r|-\beta/2, & |r|\ge\beta
    \end{cases}
  \]
  である。外れ値の影響を二乗誤差より抑えつつ、誤差が小さい領域では滑らかに最適化できる。
  \item シグモイド関数とは、実数 $z$ を 0 から 1 の範囲に写す関数
  \[
    \sigma(z)=\frac{1}{1+\exp(-z)}
  \]
  である。関数形は S 字型であり、$\sigma(0)=0.5$、$z\rightarrow+\infty$ で $\sigma(z)\rightarrow1$、$z\rightarrow-\infty$ で $\sigma(z)\rightarrow0$ となる。
  二値分類では、$\sigma(z)$ を正例の確率として扱う。
  \item ロジット（logit）とは、確率 $p$ を実数に写す変換
  \[
    \mathrm{logit}(p)=\log\frac{p}{1-p}
  \]
  である。定義域は $0<p<1$ であり、$\mathrm{logit}(0.5)=0$、$p\rightarrow1$ で $+\infty$、$p\rightarrow0$ で $-\infty$ へ発散する。
  数学的には、logit は sigmoid の逆関数である。
  すなわち、
  \[
    \mathrm{logit}(\sigma(z))=z,\qquad
    \sigma(\mathrm{logit}(p))=p
  \]
  が成り立つ。
  ニューラルネットワークでは、確率に logit 関数を適用した値だけでなく、シグモイド関数に入れる前の実数出力 $z$ そのものもロジットと呼ぶ。
  本解析で「ロジット」と書く場合、通常はモデルが直接出力する生の値 $z$ を指す。
  確率として解釈するときだけ $\sigma(z)$ を計算する。
  質量分類では、$z$ が大きいほど鉄らしく、$z=0$ が $p(\mathrm{Fe})=0.5$ に対応する。
  \item 多層パーセプトロン（Multi-Layer Perceptron: MLP）とは、全結合層と非線形活性化関数を重ねたニューラルネットワークである。
  \item 畳み込みニューラルネットワーク（Convolutional Neural Network: CNN）とは、近接する入力点を共有重みで処理するニューラルネットワークである。本モデルでは、検出器平面ではなく、各パルスノードに付いた 1 次元波形に対して CNN を用いる。
  \item ゲート付き再帰ユニット（Gated Recurrent Unit: GRU）とは、時系列を順に処理する再帰型ニューラルネットワークである。波形の時間構造を要約するために用いる。
  \item プーリングとは、複数のベクトルを 1 つのベクトルに要約する処理である。本モデルでは、平均プーリング、最大プーリング、注意機構付きプーリングを用いる。
  \item 注意機構（attention）とは、入力要素ごとに重みを学習し、重要な要素を強く反映する仕組みである。
  \item ドロップアウトとは、学習中に一部の中間成分を確率的に無効化する正則化である。過学習を抑える目的で用いる。
  \item 層正規化（layer normalization: LayerNorm）とは、層の出力を正規化して学習を安定させる処理である。
  \item softplus 関数とは、$\log(1+\exp x)$ で定義される滑らかな正値化関数である。予想誤差のように負になってはいけない量を出力するときに用いる。
  \item 負の対数尤度（Negative Log-Likelihood: NLL）とは、確率モデルの尤もらしさを損失として表す量である。比較実験では、事象ごとの再構成誤差の大きさを同時に学習するために用いた。
  \item 二値交差エントロピー（Binary Cross Entropy: BCE）とは、二値分類で用いる標準的な損失関数である。
  本モデルでは、陽子と鉄の分類、および品質スコアの学習に用いる。
  質量分類では教師値 $y$ は陽子 $0$、鉄 $1$ の二値ラベルである。
  一方、品質スコアでは教師値は二値ラベルではなく、後述する連続値 $q_{\mathrm{target}}\in[0,1]$ である。
  ロジット $z$ と教師値 $y\in[0,1]$ に対するロジット付き BCE は
  \[
    \mathrm{BCEWithLogits}(z,y)
    =
    -y\log\sigma(z)-(1-y)\log(1-\sigma(z))
  \]
  である。
  ここで入力は確率 $p$ ではなく、生のロジット $z$ である。
  実装では数値的に安定な形で、シグモイドと BCE をまとめて計算する。
  $0<y<1$ の soft target に対しては、損失は $\sigma(z)=y$、すなわち $z=\mathrm{logit}(y)$ で最小になる。
  \item Root Mean Square Error（RMSE）とは、二乗平均平方根誤差である。$N$ 個の誤差 $r_i$ に対して
  \[
    \mathrm{RMSE}=\sqrt{\frac{1}{N}\sum_i r_i^2}
  \]
  と定義する。
  \item 中央 68\% 幅とは、誤差分布の 16 パーセンタイルから 84 パーセンタイルまでの幅である。半幅はその半分である。正規分布なら 1 標準偏差に対応するが、本解析では分布の形を仮定しないロバストな分解能指標として使う。
\end{itemize}

図~\ref{fig:function-shapes} に、sigmoid、logit、SmoothL1、BCEWithLogits の具体的な関数形を示す。
この図は \code{docs/model\_architecture/plot\_function\_shapes.py} で生成した。

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.95\linewidth]{fig/function_shapes.pdf}
  \caption{本書で使う主な非線形関数と損失関数の形。sigmoid はロジット $z$ を確率に写し、logit は確率 $p$ を実数に戻す。SmoothL1 は原点付近で二乗誤差、大きな誤差で絶対値誤差として振る舞う。BCEWithLogits の図では、品質スコアで用いる soft target の例として $q_{\mathrm{target}}=0.1,0.5,0.9$ を示す。点線は各 soft target に対して損失が最小になる $z=\mathrm{logit}(q_{\mathrm{target}})$ を表す。}
  \label{fig:function-shapes}
\end{figure}

\section{事象グラフ}

1 つの空気シャワー事象を、1 つの有向グラフとして表す。
ここでいうグラフは、検出器を規則格子に並べた画像ではない。
Ising 型ノイズフィルタとは、パルス候補の採用・非採用を二値変数として扱い、時空間的に整合しないノイズ候補を落とす前処理である。
この前処理で残ったパルス候補を accepted pulse（採用パルス）と呼ぶ。
本モデルでは、accepted pulse をノードとし、検出器間の空間的・時間的な関係をエッジとする疎な不規則グラフを用いる。
同じ検出器に複数の accepted pulse が残る場合、その検出器は複数のノードを持つ。
ただし、パルスノード数が 4 未満、または異なる検出器数が 4 未満の事象は学習に使わない。

今回の解析に対応する事象単位のグラフ可視化 PDF は、本書用にはまだ生成していない。
\code{fig/mc\_ising\_filter\_event.pdf} と \code{fig/ising\_edge\_candidate\_graph.pdf} は、全検出器を結ぶ確認図または TALE+infill の別解析に対応する図であり、本解析の事象グラフ図としては使わない。
本解析の事象グラフを本文に入れる場合は、現在の HDF5 グラフから \code{talesd-gnn visualize} で生成した図だけを用いる。

1 事象の HDF5 グループには、次のテンソルを保存する。
\[
  \mathbf{X}\in\R^{N_v\times28},\quad
  \mathbf{A}\in\R^{N_e\times13},\quad
  \mathbf{P}_{\mathrm{raw}}\in\R^{N_p\times1},\quad
  \mathbf{W}\in\R^{N_v\times4\times128}.
\]
ここで、$N_v$ は accepted-pulse ノード数、$N_e$ は有向エッジ数である。
$\mathbf{P}_{\mathrm{raw}}$ の第 1 列は、対応するノード番号である。
現行仕様では追加のパルススカラー特徴量は使わず、パルスエンコーダは無効である。

\subsection{ノード特徴量}

ここでいうノード特徴量とは、accepted-pulse ノード 1 つに付く 28 個のスカラーである。
行の単位は検出器ではなく accepted pulse である。
ただし、各 accepted-pulse ノードには、そのパルス自身の情報だけでなく、対応する検出器に由来する情報と、事象内での文脈情報も付ける。
したがって、「パルスの情報」と「検出器の情報」は次のように区別して読む。
\begin{itemize}
  \item 検出器由来の特徴量: 検出器位置、局所的な検出器間隔、検出器トリガー時刻、pedestal、pedestal sigma、波形長、波形セグメント数、FADC peak。
  \item パルス由来の特徴量: accepted pulse 自身の到達時刻、信号密度 $\rho$、同一検出器内での accepted pulse order、first-pulse flag。
  \item 検出器内パルス文脈: 同一検出器内の accepted pulse 数、accepted pulse time span、最大 accepted pulse 信号量、accepted pulse 合計信号量。
  \item 事象文脈の特徴量: 信号量重み付き重心から見た相対位置。
\end{itemize}
これらを accepted-pulse node scalar features として 1 行にまとめ、GNN のノード入力にする。
列の順序は \code{NODE\_FEATURE\_COLUMNS} で固定されており、次の通りである。
\[
\begin{gathered}
x,y,z,\ d_{\mathrm{nearest}},\ \overline{d}_{3},\ N_{\mathrm{neigh}},
\Delta x,\Delta y,\Delta z,r,\\
t_{\mathrm{pulse}},t_{\mathrm{trig}},\log_{10}\rho_{\mathrm{pulse}},\sqrt{\rho_{\mathrm{pulse}}},
\log_{10}\rho_{\max}^{\mathrm{det}},\log_{10}\rho_{\Sigma}^{\mathrm{det}},\sqrt{\rho_{\Sigma}^{\mathrm{det}}},\\
N_{\mathrm{pulse}}^{\mathrm{det}},\Delta t_{\mathrm{pulse}}^{\mathrm{det}},N_{\mathrm{segment}}^{\mathrm{det}},T_{\mathrm{wf}}^{\mathrm{det}},
\log_{10}\mathrm{FADC}_{\max},
p_u,p_l,\sigma_u,\sigma_l,
\mathrm{accepted\ pulse\ order},\mathrm{first\ accepted\ pulse\ flag}.
\end{gathered}
\]
距離の単位は km、時刻の単位は $\mu\mathrm{s}$ である。
信号量は上下層の Flash Analog-to-Digital Converter（FADC）波形から求める。
また、信号量の一部は Vertical Equivalent Muon（VEM）相当の単位で較正される。

現行の標準設定では、任意の検出器番号埋め込みを用いない。
すなわち、\code{DETECTOR\_EMBEDDING\_DIM=0} である。
検出器ごとの意味の違いは、検出器番号そのものではなく、物理座標、局所的な検出器間隔、較正値、ノイズ量、波形応答、グラフの接続関係から学習させる。

表~\ref{tab:node-feature-definitions} に、各 accepted-pulse ノード特徴量の定義と採用理由を示す。
ここで「必要性」とは、物理的に入れる理由があるかどうかを示すものであり、最終的な有効性を保証するものではない。
各特徴量群の本当の必要性は、まず学習済みモデルに対する事後解析で確認する。
その上で、性能と説明性の両方に影響する候補だけを、少数の再学習で最終確認する。
波形エンコーダを導入した後は、波形から読める量と手作り特徴量の重複を明示的に調べる必要がある。

{\small
\setlength{\tabcolsep}{3pt}
\begin{longtable}{p{25mm}p{48mm}p{50mm}p{35mm}}
  \caption{accepted-pulse ノード特徴量の定義、採用理由、および必要性の位置づけ。}
  \label{tab:node-feature-definitions}\\
  \toprule
  特徴量 & 定義 & 採用理由 & 必要性の位置づけ \\
  \midrule
  \endfirsthead
  \toprule
  特徴量 & 定義 & 採用理由 & 必要性の位置づけ \\
  \midrule
  \endhead
  \bottomrule
  \endfoot
  $x,y,z$ &
  TALE 局所座標系での検出器位置。単位は km。 &
  TALE-SD は検出器配置が不均一である。絶対位置は、どの検出器が空間的にどの役割を持つかを表す。 &
  重要度が高い。検出器 ID 埋め込みを使わないため、配置情報の主要な入力である。 \\
  $d_{\mathrm{nearest}}$ &
  同じ事象に含まれる異なる検出器のうち、最近接検出器までの水平距離。 &
  局所的な検出器間隔を表す。疎な領域と密な領域では、同じ信号パターンでも再構成の制約が異なる。 &
  有用候補。グラフ構造にも近接情報は含まれるため、冗長性は ablation test で確認する。 \\
  $\overline{d}_{3}$ &
  近い方から最大 3 台までの検出器距離の平均。 &
  最近接距離だけでは局所配置の広がりを表しにくい。複数近傍の平均により局所的なサンプリング密度を補う。 &
  有用候補。$d_{\mathrm{nearest}}$ や $N_{\mathrm{neigh}}$ との重複を検証する。 \\
  $N_{\mathrm{neigh}}$ &
  半径 $1.5\,\mathrm{km}$ 以内にある同一事象内の近傍検出器数。 &
  エッジ候補の多さと局所的な観測制約の強さを表す。 &
  有用候補。低 multiplicity 事象での寄与を評価する。 \\
  $\Delta x,\Delta y,\Delta z$ &
  信号量 $\rho$ で重み付けした事象内重心から見たノード位置。 &
  シャワー軸やコア位置は、事象内の相対的な信号配置に強く依存する。絶対座標だけではこの情報が直接表れない。 &
  重要度が高い。再構成に直接関係するが、重心の定義依存性は検証が必要である。 \\
  $r$ &
  事象内重心からの水平・鉛直を含む距離 $|\Delta \mathbf{x}|$。 &
  信号の横方向分布と到達時刻の広がりを表す簡潔な量である。 &
  有用候補。$\Delta x,\Delta y,\Delta z$ から計算できるため、冗長性がある。 \\
  $t_{\mathrm{pulse}}$ &
  その accepted pulse の到達時刻から、事象内で最も早い accepted pulse の時刻を引いた値。 &
  シャワー面の到達順序を表す。方向再構成に直接関係する。 &
  重要度が高い。角度精度に効く主要な時刻入力である。 \\
  $t_{\mathrm{trig}}$ &
  イベント基準時刻から見た検出器トリガー時刻。 &
  accepted pulse 時刻とは別に、検出器単位のトリガー時刻を与える。波形切り出しや時刻基準のずれに関する情報を持つ。 &
  有用候補。$t_{\mathrm{pulse}}$ との重複度を検証する。 \\
  $\log_{10}\rho_{\mathrm{pulse}}$,
  $\sqrt{\rho_{\mathrm{pulse}}}$ &
  その accepted pulse 自身の信号密度 $\rho$ の対数と平方根。 &
  信号量はエネルギーとコア距離に関係する。対数と平方根を併用することで、広いダイナミックレンジを扱いやすくする。 &
  重要度が高い。ただし、対数と平方根の両方が必要かは検証が必要である。 \\
  $\log_{10}\rho_{\max}^{\mathrm{det}}$ &
  同じ検出器に残った accepted pulse のうち、最大信号密度の対数。 &
  複数パルスがある検出器で、その検出器が持つ最大信号の大きさを表す。 &
  有用候補。波形入力との重複度を検証する。 \\
  $\log_{10}\rho_{\Sigma}^{\mathrm{det}}$,
  $\sqrt{\rho_{\Sigma}^{\mathrm{det}}}$ &
  同じ検出器に残った accepted pulse の信号密度を合計した量の対数と平方根。 &
  検出器で観測された総信号量を表す。単一パルスだけを見る場合より、パルス分裂がある検出器の信号規模を自然に表せる。 &
  重要度が高い。コア距離とエネルギーに効くが、波形積分量との重複を確認する。 \\
  $N_{\mathrm{pulse}}^{\mathrm{det}}$ &
  同じ検出器に残った accepted pulse の数。 &
  パルスの分裂、後続パルス、ノイズ残りの程度を表す。核種分類では波形形状とともに評価する。 &
  有用候補。波形エンコーダとの重複を確認する。 \\
  $\Delta t_{\mathrm{pulse}}^{\mathrm{det}}$ &
  同じ検出器内の最終 accepted pulse と先頭 accepted pulse の時刻差。 &
  パルス列の時間的な広がりを表す。シャワーの厚みやノイズ残りに関係する量として評価する。 &
  有用候補。質量分類で特に検証すべきである。 \\
  $N_{\mathrm{segment}}^{\mathrm{det}}$,
  $T_{\mathrm{wf}}^{\mathrm{det}}$ &
  波形セグメント数と、波形長を $\mu\mathrm{s}$ 単位に変換した量。 &
  波形取得条件の違いをモデルに知らせる。入力波形の欠落や長さの違いを補正する役割を持つ。 &
  補助量。学習データでほぼ一定なら不要である。 \\
  $\log_{10}\mathrm{FADC}_{\max}$ &
  上下層 FADC 波形の最大カウント値の対数。 &
  飽和に近い波形や大信号事象を示す。信号量の信頼性を判断する補助情報である。 &
  補助量。飽和事象を除外している場合は重要度が下がる。 \\
  $p_u,p_l$ &
  上層、下層の pedestal 平均値。 &
  波形の基線を表す。較正状態やノイズ環境の違いを示す。 &
  補助量。波形が十分に較正されていれば削除候補である。 \\
  $\sigma_u,\sigma_l$ &
  上層、下層の pedestal 標準偏差。 &
  電子ノイズや基線揺らぎの大きさを表す。信号の信頼性とノイズ残りに関係する。 &
  有用候補。低信号事象と質量分類での寄与を評価する。 \\
  accepted pulse order &
  同じ検出器内での accepted pulse の順序。先頭を 0 とする。 &
  複数パルスがある場合、先頭パルスと後続パルスを区別する。 &
  有用候補。時系列波形と重複するため検証が必要である。 \\
  first accepted pulse flag &
  同じ検出器内の先頭 accepted pulse なら 1、それ以外なら 0。 &
  先頭パルスを明示する二値特徴量である。到来時刻と後続パルスを区別しやすくする。 &
  有用候補。pulse order との重複を検証する。 \\
\end{longtable}
}

この表から分かるように、すべての特徴量が同じ意味で「必須」ではない。
$x,y,z$、相対位置、時刻、信号量は、再構成に直接関係するため優先度が高い。
一方、pedestal、パルス数、波形長などは補助情報であり、低信号事象、低 multiplicity 事象、質量分類での寄与を個別に評価する。
以前の HDF5 には \code{log10\_total\_rho} と \code{sqrt\_total\_rho} という名前の列が含まれていたが、これは現在の \code{detector\_sum\_rho} と同じ物理定義ではないため、現行入力へは読み替えない。
また、\code{local\_detector\_density\_1p5km2} は
\code{neighbor\_count\_1p5km}/\((\pi\times1.5^2)\)
で定義される完全な定数倍なので、現行入力から削除した。
特徴量の物理定義を変更した後は、既存 HDF5 を現行 schema として使わず、DST から再 export する。
さらに、旧 \code{pulse\_features} の6個のスカラーは現行の accepted-pulse node 特徴量と完全に重複するため、現行仕様では \code{node\_index} だけを保持し、パルスエンコーダは使わない。

特徴量の必要性は、まず学習済みチェックポイントに対する post-hoc 解析で調べる。
具体的には、特徴量を物理的に関連する group に分け、検証データまたはテストデータで group ablation または permutation を行い、再構成指標の悪化量を測る。
重要度は
\[
  I_g = M(\mathrm{perturb}(g))-M(\mathrm{original})
\]
で定義する。
ここで $g$ は特徴量 group、$M$ は \code{rmse\_log10\_energy}、\code{core\_68\_km}、\code{angular\_68\_deg} などの評価指標である。
この方法は再学習を必要としないため、\code{flat50000} のように 1 回の学習が半日から 1 日かかる条件でも実行できる。

最初に評価する group は次の通りである。
\begin{itemize}
  \item signal size group: $\rho$、$\log_{10}\rho$、$\sqrt{\rho}$、波形積分量、波形ピーク。
  \item timing group: ノード到来時刻、トリガー時刻、エッジの $\Delta t$、$|\Delta t|$、$\Delta t/d$。
  \item geometry group: 絶対位置、重心相対位置、エッジ距離。
  \item graph structure group: ノード数、エッジ数、近傍数、局所密度。
  \item waveform shape group: 上下層、raw/accepted、時間窓の形状。
  \item calibration and noise group: pedestal、pedestal sigma、FADC peak。
\end{itemize}

相関の強い特徴量は、個別に独立寄与を主張しない。
例えば \code{log10\_pulse\_rho} と \code{sqrt\_pulse\_rho} は同じ信号量の異なる表現であり、まず signal size group として評価する。
group が効くことを確認した後で、group 内の冗長性を conditional permutation または少数の reduced-feature 再学習で確認する。
性能が変わらず説明性が上がる場合は削除し、性能が落ちる場合、または有限データ・有限モデルで安定化に寄与する場合は残す。

\subsection{パルス特徴量}

$\mathbf{P}_{\mathrm{raw}}$ は、accepted pulse と node を対応させるための index 配列として保持する。
現行仕様では各 accepted pulse がそのまま node であり、\code{pulse\_features} は \code{node\_index} だけを持つ。
以前の HDF5 には到達時刻、信号量、pulse order、first pulse flag など6個のパルススカラーが保存されていた。
しかし、これらは同じ accepted-pulse node の \code{pulse\_arrival\_usec\_rel}、\code{log10\_pulse\_rho}、\code{sqrt\_pulse\_rho}、\code{accepted\_pulse\_order}、\code{is\_first\_accepted\_pulse} と完全に重複する。
したがって、現行の loader はこれらの旧パルススカラーを落とし、モデルには渡さない。

\subsection{波形特徴量}

各 accepted-pulse ノードには、長さ 128 bin の 4 チャネル波形を付ける。
\[
\mathbf{W}_v =
\left(
W^{u}_{\mathrm{raw}},
W^{l}_{\mathrm{raw}},
M^{u}_{\mathrm{accepted}},
M^{l}_{\mathrm{accepted}}
\right)\in\R^{4\times128}.
\]
$u$ は上層、$l$ は下層を表す。
1 bin は $0.02\,\mu\mathrm{s}$ であり、128 bin は $2.56\,\mu\mathrm{s}$ に対応する。
raw 波形値は pedestal を引いた後、VEM 相当の単位へ較正し、極端な値はクリップして保存する。
4 チャネルの意味は表~\ref{tab:waveform-channel-definitions} の通りである。

\begin{table}[htbp]
  \centering
  \small
  \caption{波形 4 チャネルの定義と役割。}
  \label{tab:waveform-channel-definitions}
  \begin{tabular}{p{28mm}p{58mm}p{58mm}}
    \toprule
    チャネル & 定義 & 役割 \\
    \midrule
    $W^u_{\mathrm{raw}}$ &
    上層 FADC 波形を、対象 accepted pulse の立ち上がりで切り出した窓。 &
    上層の局所的な立ち上がり、幅、尾、基線を表す。 \\
    $W^l_{\mathrm{raw}}$ &
    下層 FADC 波形を、対象 accepted pulse の立ち上がりで切り出した窓。 &
    下層の局所的な立ち上がり、幅、尾、基線を表す。 \\
    $M^u_{\mathrm{accepted}}$ &
    上層 raw 窓と同じ時間軸で、accepted pulse 区間を 1、それ以外を 0 とする mask。 &
    raw 波形中のどの bin が上層 accepted pulse として採用されたかを明示する。 \\
    $M^l_{\mathrm{accepted}}$ &
    下層 raw 窓と同じ時間軸で、accepted pulse 区間を 1、それ以外を 0 とする mask。 &
    raw 波形中のどの bin が下層 accepted pulse として採用されたかを明示する。 \\
    \bottomrule
  \end{tabular}
\end{table}

raw チャネルは、accepted pulse の立ち上がりを基準に切り出した較正済み波形窓である。
基準 bin は、上層と下層で検出された立ち上がり bin のうち早い方、
\[
  b_{\mathrm{rise}}=\min(b^{u}_{\mathrm{rise}},b^{l}_{\mathrm{rise}})
\]
を指す。
実装では、この bin の 32 bin 前から長さ 128 bin を切り出す。
したがって、波形の範囲内に十分な pre-pulse 領域がある場合、$b_{\mathrm{rise}}$ は出力窓の配列添字 32 の bin に対応する。
元の波形の先頭または末尾を越える部分は 0 で埋める。
この raw チャネルは、各 accepted pulse ノードごとに作られる。
この基準は、検出器間の絶対到達時刻をそろえるためではない。
検出器間の時刻差は、$t_{\mathrm{first}}$ やエッジの $\Delta t$ として別に入力する。
raw 波形窓では、各ノード内のパルス立ち上がり前後の形状を比較しやすくすることを目的として、立ち上がり位置を固定する。
したがって raw チャネルは、同じ検出器に複数の accepted pulse がある場合でも、対象ノードごとに異なる窓になる。

accepted mask チャネルは、raw チャネルと同じ切り出し窓上で定義する。
Ising 型ノイズフィルタ後に残った accepted pulse 区間を 1、それ以外を 0 とする。
したがって、モデルには通常の較正済み raw 波形と、どの bin を accepted pulse とみなしたかだけを別々に渡す。
gap や非 accepted 区間を「0 の波形値」として表現しないため、波形振幅と pulse 採用判定を混同しにくい。
同じ検出器に複数の accepted pulse ノードがある場合でも、mask は対象ノードの raw 窓にそろえて作るため、検出器全体の accepted pulse 列を全ノードに重複して渡さない。
波形値はグラフ作成時に較正とクリップを行うが、学習時の標準化スケーラでは標準化しない。
この波形定義は HDF5 属性 \code{waveform\_schema=rise\_aligned\_raw\_plus\_accepted\_mask\_v1} として保存する。
古い accepted-pulse compact 波形または accepted-gapped 波形を含む HDF5 は、この定義と物理的に等価ではないため、学習時にエラーとして扱う。

\subsection{エッジ特徴量}

エッジは、異なる検出器 ID を持つノード間にだけ張る。
検出器間距離が $1.5\,\mathrm{km}$ 以下であり、かつ時刻差が $|\Delta t|\le 8\,\mu\mathrm{s}$ の組を候補とする。
採用した無向の組は、2 本の有向エッジとして保存する。
エッジ特徴量は 13 列であり、次の量から成る。
\[
(\Delta x,\Delta y,\Delta z,d,\Delta t,|\Delta t|,\Delta t/d,
\Delta\log_{10}\rho,
w_{\mathrm{ising}},w_{\mathrm{raw}},t_{\mathrm{excess}},s_{\mathrm{space}},s_{\mathrm{causal}}).
\]
ここで、エッジ $i\rightarrow j$ では
\[
  \Delta \mathbf{x}_{ij}=\mathbf{x}_j-\mathbf{x}_i,\qquad
  \Delta t_{ij}=t_j-t_i,\qquad
  \Delta\log_{10}\rho_{ij}=\log_{10}\rho_j-\log_{10}\rho_i
\]
である。
反対向きのエッジ $j\rightarrow i$ では、符号付きの量は符号が反転する。
距離 $d$、$|\Delta t|$、$t_{\mathrm{excess}}$、$s_{\mathrm{space}}$、$s_{\mathrm{causal}}$ は向きによらない。
ここで $s$ は score の意味で用いており、学習対象のパラメータではない。
具体的には
\[
  s_{\mathrm{space}}=\exp(-d/\lambda_{\mathrm{space}}),\qquad
  s_{\mathrm{causal}}=\exp(-t_{\mathrm{excess}}/\tau_{\mathrm{causal}})
\]
であり、現行実装では $\lambda_{\mathrm{space}}=0.8\,\mathrm{km}$、$\tau_{\mathrm{causal}}=1.0\,\mu\mathrm{s}$ を用いる。
$t_{\mathrm{excess}}$ は、光速伝播時間に $0.3\,\mu\mathrm{s}$ の許容幅を加えた後に残る余剰時刻差である。
各列の意味を表~\ref{tab:edge-feature-definitions} にまとめる。

\begin{table}[p]
  \centering
  \small
  \caption{エッジ特徴量の定義。エッジは異なる検出器の accepted-pulse ノード間にだけ張る。}
  \label{tab:edge-feature-definitions}
  \begin{tabular}{lll}
    \toprule
    特徴量 & 定義 & 役割 \\
    \midrule
    $\Delta x,\Delta y,\Delta z$ &
    始点ノードから終点ノードへの位置差。 &
    シャワー面が検出器間をどの向きに横切ったかを表す。 \\
    $d$ &
    位置差のユークリッド距離 $|\Delta\mathbf{x}|$。 &
    時刻差を距離で解釈するための基準になる。 \\
    $\Delta t$ &
    終点到達時刻から始点到達時刻を引いた符号付き時刻差。 &
    到達順序を表し、方向再構成に直接効く。 \\
    $|\Delta t|$ &
    時刻差の絶対値。 &
    向きに依存しない時刻の近さを表す。 \\
    $\Delta t/d$ &
    距離あたりの符号付き時刻差。 &
    局所的な到達面の傾きを表す。 \\
    $\Delta\log_{10}\rho$ &
    終点と始点の信号量対数差。 &
    コアに近い側と遠い側の信号勾配を表す。 \\
    $w_{\mathrm{raw}}$ &
    空間近接度、因果整合性、信号量から作る未正規化重み。 &
    Ising 型フィルタで使う時空間的な整合度を表す。 \\
    $w_{\mathrm{ising}}$ &
    $w_{\mathrm{raw}}$ を正の接続次数で規格化した重み。 &
    高次数ノードに重みが集中しすぎることを抑える。 \\
    $t_{\mathrm{excess}}$ &
    $ \max(0,|\Delta t|-d/c-0.3\,\mu\mathrm{s}) $。 &
    光速伝播から見て遅すぎる時刻差を表す。 \\
    $s_{\mathrm{space}}$ &
    $\exp(-d/0.8\,\mathrm{km})$。 &
    近い検出器対を強く結ぶ空間スコア。 \\
    $s_{\mathrm{causal}}$ &
    $\exp(-t_{\mathrm{excess}}/1.0\,\mu\mathrm{s})$。 &
    因果的に自然な時刻差を強くするスコア。 \\
    \bottomrule
  \end{tabular}
\end{table}

$w_{\mathrm{raw}}$ は
\[
  w_{\mathrm{raw}}
  =
  s_{\mathrm{space}}s_{\mathrm{causal}}
  \left[
    1+0.12\sqrt{\log(1+\rho_i)\log(1+\rho_j)}
  \right]
\]
で定義する。
$w_{\mathrm{ising}}$ は、正の接続次数 $D_i,D_j$ を用いて
\[
  w_{\mathrm{ising}} =
  \frac{w_{\mathrm{raw}}}{(D_iD_j)^{0.35}}
\]
とする。
同じ検出器内の複数パルス間にはエッジを張らない。
検出器内のパルス順序と時間幅は、パルス特徴量、検出器単位の accepted-pulse 集約量、raw 波形、accepted mask で表す。
シャワー面の幾何を決めるうえで、検出器間の時刻差は重要である。
そのため、現行モデルでは $\Delta t$、$|\Delta t|$、$\Delta t/d$ を通常のエッジ特徴量として使うだけでなく、専用の時刻差エンコーダにも入力する。

\subsection{教師ラベル}

再構成の教師ラベルは
\[
\mathbf{y}_{\mathrm{reco}}=
(\log_{10}E,\ x_c,y_c,\ n_x,n_y,n_z)\in\R^6
\]
である。
$E$ は eV 単位のエネルギー、$(x_c,y_c)$ は km 単位の地表面上のコア位置である。
到来方向の自由度は 2 であるが、方位角の周期境界を避けるため、実装では $(n_x,n_y,n_z)$ という 3 成分単位ベクトルで表す。

質量分類の教師ラベルは、\code{rusdmc} に記録された CORSIKA（COsmic Ray SImulations for KAscade）粒子番号から作る。
陽子は粒子番号 14 であり、ラベル 0 とする。
鉄は粒子番号 5626 であり、ラベル 1 とする。

\section{前処理とデータ分割}

ノード特徴量、エッジ特徴量、再構成ターゲットには、学習データだけから求めた標準化を適用する。
現行仕様では追加のパルススカラー特徴量がないため、パルス用の標準化器は作らない。
標準化は
\[
  x'=(x-\mu_{\mathrm{train}})/\sigma_{\mathrm{train}}
\]
で定義する。
ここで、$\mu_{\mathrm{train}}$ と $\sigma_{\mathrm{train}}$ は学習データにおける平均と標準偏差である。
標準偏差が数値的に 0 に近い列では、標準偏差を 1 として扱う。

モデルは標準化後のターゲットを出力する。
ただし、物理量に基づく損失関数と性能評価では、予測値と教師ラベルを物理単位に戻してから、エネルギー誤差、コア位置誤差、角度誤差を計算する。
この処理により、学習の数値安定性と、物理的に解釈しやすい損失の両方を保つ。

標準のデータ分割は \code{source-stratified} である。
これは、同じソースパスに由来する事象が異なる分割に混ざりにくいようにしながら、物理パラメータの分布をできるだけ保つ分割である。
分割は、ソースパス単位で行う。
ただし、比率はソースパス数だけではなく、各ソースパスに含まれるグラフ数に近づくように決める。
これは、1 つの DST ファイルに含まれる再構成可能事象数が異なる場合に、学習、検証、テストの実イベント数が大きく崩れることを避けるためである。
標準設定では、イベント数は検証データを $5\%$、テストデータを $10\%$、残りを学習に使うことを目標にする。
一方で、source path 数は学習、検証、テストをおよそ $70\%$、$10\%$、$20\%$ に分けることを目標にする。
この 2 つは別の制約であり、source path 非リークを守るため、実イベント比率は目標値からずれる場合がある。
現行 \code{flat50000\_wfmask\_target6\_20260603\_102418} では、イベント比率は学習 81.0\%、検証 7.37\%、テスト 11.6\%、source path 比率は学習 69.3\%、検証 10.1\%、テスト 20.6\% である。
学習時には、各エポックの開始時に学習インデックス全体をシャッフルしてからバッチを作る。
検証データとテストデータの順序は固定する。
この設計は、HDF5 内のエネルギー順、ソース順、粒子種順がバッチ内に残ることを防ぐためのものである。

\section{入力分布の確認図}

図~\ref{fig:input-node}、図~\ref{fig:input-edge}、図~\ref{fig:input-pulse-waveform-target} は、現行 HDF5 \code{flat50000\_wfmask\_target6\_20260603\_102418} から作成した入力分布である。
この HDF5 は 2,839,184 事象を含み、図はそのうち 100,000 事象をサンプルして作成した。
現行 schema では \code{log10\_pulse\_rho}、\code{log10\_detector\_sum\_pulse\_rho}、\code{detector\_accepted\_pulse\_count} のように、パルス自身の量、検出器内 accepted pulse 集約量、事象全体の文脈を分けて保存する。
波形は \code{upper\_raw\_window\_vem}、\code{lower\_raw\_window\_vem}、\code{upper\_accepted\_mask}、\code{lower\_accepted\_mask} の 4 channel である。

\code{pulse\_features} には \code{node\_index} だけを残す。
これは accepted pulse と node の対応を保持するための index であり、追加の pulse scalar 入力ではない。
そのため、現行モデルでは pulse scalar encoder を使わず、TeX 本文にも pulse scalar 分布図は載せない。
\code{detector\_wf\_segments} と \code{detector\_wf\_length\_usec} は、波形長や複数 segment の情報を表す検出器単位の入力として保持する。
これらを削るかどうかは、feature importance と reduced-feature 再学習で確認する。
図~\ref{fig:split-distributions} には、同じ HDF5 に対する split 後の energy bin count と主要物理量分布を示す。

\begin{figure}[p]
  \centering
  \includegraphics[width=0.95\linewidth]{fig/input_distributions/node_features.pdf}
  \caption{現行 \code{flat50000\_wfmask\_target6\_20260603\_102418} HDF5 から作成した accepted-pulse ノード特徴量の分布。}
  \label{fig:input-node}
\end{figure}

\begin{figure}[p]
  \centering
  \includegraphics[width=0.95\linewidth]{fig/input_distributions/edge_features.pdf}
  \caption{現行 \code{flat50000\_wfmask\_target6\_20260603\_102418} HDF5 から作成したエッジ特徴量の分布。}
  \label{fig:input-edge}
\end{figure}

\begin{figure}[p]
  \centering
  \includegraphics[width=0.95\linewidth]{fig/input_distributions/waveform_features.pdf}
  \vspace{2mm}
  \includegraphics[width=0.95\linewidth]{fig/input_distributions/target_features.pdf}
  \caption{現行 \code{flat50000\_wfmask\_target6\_20260603\_102418} HDF5 から作成した波形特徴量と再構成ターゲットの分布。}
  \label{fig:input-pulse-waveform-target}
\end{figure}

\begin{figure}[p]
  \centering
  \includegraphics[width=0.95\linewidth]{fig/split_distributions/split_energy_bin_counts.pdf}
  \vspace{2mm}
  \includegraphics[width=0.95\linewidth]{fig/split_distributions/split_parameter_distributions.pdf}
  \caption{現行 \code{flat50000\_wfmask\_target6\_20260603\_102418} HDF5 の train/validation/test 分布。source path 非リークを保つため、実イベント比率は目標比率からずれる。}
  \label{fig:split-distributions}
\end{figure}

\clearpage
\section{共通のネットワーク構造}

再構成と質量分類のみの学習は、どちらも標準で \code{PhysicsTaleSdGNN} を用いる。
表~\ref{tab:common-defaults} に、サーバー学習で用いる主な設定を示す。

\begin{table}[htbp]
  \centering
  \caption{共通の標準設定。}
  \label{tab:common-defaults}
  \begin{tabular}{lll}
    \toprule
    項目 & 標準値 & 意味 \\
    \midrule
    構造 & \code{physics} & エネルギー、コア、方向を別の出力層で予測する GNN \\
    隠れ次元 $H$ & 192 & ノード状態と事象ベクトルの基本次元 \\
    GNN 層数 $L_g$ & 5 & ゲート付きメッセージ伝播層の数 \\
    読み出しヘッド数 $K$ & 4 & 注意機構付きグラフプーリングの数 \\
    ドロップアウト率 & 0.05 & MLP 内で使う正則化の強さ \\
    波形エンコーダ & \code{cnn-gru} & 1 次元 CNN と双方向 GRU による波形要約 \\
    波形埋め込み次元 $D_w$ & 64 & 1 ノードあたりの波形表現の次元 \\
    検出器 ID 埋め込み & 0 & 標準設定では使用しない \\
    バッチサイズ & 256 & 1 回の更新で使うグラフ数 \\
    最適化手法 & AdamW & Adam with Decoupled Weight Decay。学習率 $3\times10^{-4}$、重み減衰 $10^{-4}$ \\
    学習率スケジュール & reduce-on-plateau & 検証損失の改善が止まったときに学習率を下げる \\
    \bottomrule
  \end{tabular}
\end{table}

\subsection{ノードごとのエンコーダ}

現行仕様では、追加のパルススカラー特徴量は使わない。
\code{pulse\_features} は対応する accepted-pulse node を示す \code{node\_index} だけを保持し、\code{pulse\_dim=0} となる。
したがって、パルスエンコーダとパルス表現の平均・最大プーリングは無効である。

波形エンコーダは、まず次の 1 次元畳み込みを適用する。
\[
\mathrm{Conv1D}(4\rightarrow32,k=5)\rightarrow\mathrm{SiLU}
\rightarrow\mathrm{LayerNorm}
\rightarrow\mathrm{Conv1D}(32\rightarrow64,k=5)\rightarrow\mathrm{SiLU}.
\]
標準の \code{cnn-gru} モードでは、その後に隠れ次元 32 の双方向 GRU を適用する。
GRU の最終状態、CNN 系列の平均、CNN 系列の最大値を連結し、64 次元の波形埋め込みに射影する。

ノードエンコーダは、accepted-pulse ノード特徴量と波形埋め込みを連結した入力を受け取る。
標準設定での入力次元は
\[
  28\ \oplus\ 64 = 92
\]
である。
この 92 次元の入力を
\[
  \R^{92}\rightarrow\R^{192}\rightarrow\R^{192}
\]
の MLP で 192 次元のノード状態に変換する。
各線形層の後には SiLU と LayerNorm を置く。

\subsection{時刻差エンコーダとメッセージ伝播}

メッセージ伝播の前に、時刻差エンコーダがエッジの時刻差特徴量
$(\Delta t,|\Delta t|,\Delta t/d)$
を MLP で処理する。
その出力を、到着先ノードごとに平均してノード状態に加え、LayerNorm で正規化する。
この処理は、シャワー面の到達時刻差を通常のエッジ特徴量より明示的に使わせるためのものである。

5 層の GNN は、ゲート付きエッジメッセージ伝播層で構成される。
有向エッジ $i\rightarrow j$ のエッジ特徴量を $a_{ij}$ とすると、メッセージの入力は
\[
  [h_i,h_j,a_{ij}]\in\R^{2H+13}=\R^{397}
\]
である。
メッセージ MLP はこの入力を 192 次元に写す。
別のゲート MLP はスカラーのシグモイド値を出力する。
したがって、有効なメッセージは
\[
  m_{ij}=\mathrm{MLP}_m([h_i,h_j,a_{ij}])\,
  \sigma(\mathrm{MLP}_g([h_i,h_j,a_{ij}]))
\]
である。

各ノードでは、入力してくるメッセージの平均と最大値を計算する。
ノード更新では、現在のノード状態、平均メッセージ、最大メッセージを連結した
$[h_j,\overline{m}_j,m^{\max}_j]\in\R^{3H}$
を用いる。
更新後には、残差接続、LayerNorm、フィードフォワードネットワーク（feed-forward network: FFN）を適用する。
残差接続は、入力を出力に足し戻す処理であり、深いネットワークの学習を安定させる。

\subsection{グラフ読み出し}

メッセージ伝播後のノード状態を、グラフ全体の 1 つの事象ベクトルに変換する。
この処理をグラフ読み出しと呼ぶ。
本モデルでは、全ノードの平均、全ノードの最大値、4 個の注意機構付き読み出しヘッドを連結する。
したがって、グラフ読み出しの次元は
\[
  H(2+K)=192\times6=1152
\]
である。
再構成では、この 1152 次元ベクトルを共有 MLP
\[
  \R^{1152}\rightarrow\R^{384}\rightarrow\R^{192}
\]
に通す。
得られた 192 次元の事象表現を、再構成出力と品質出力に入力する。

\begin{figure}[p]
  \centering
  \resizebox{\linewidth}{!}{\input{fig/model_architecture.tikz}}
  \caption{共通のモデル構造。検出器配置は規則格子画像としてではなく、物理特徴量とグラフエッジとして入力する。}
  \label{fig:model_architecture}
\end{figure}

\begin{figure}[p]
  \centering
  \resizebox{0.92\linewidth}{!}{\input{fig/concrete_training_model.tikz}}
  \caption{現行のサーバー学習スクリプトで用いる具体的な構造。再構成と質量分類のみの学習は、エンコーダと GNN を共有し、有効にする出力層と損失関数を切り替える。}
  \label{fig:concrete_training_model}
\end{figure}

\clearpage
\section{再構成モデル}

再構成用スクリプトは、エネルギー、コア位置、到来方向、および事象品質を学習する。
標準コマンドは
\[
\code{scripts/submit\_server\_waveform\_full\_training.sh}
\]
である。
2026 年 6 月 1 日に完了した最新の \code{flat50000} 再構成 run では、\code{TRAINING\_TASK=reconstruction}、\code{LOSS\_MODE=physics}、\code{QUALITY\_PREDICTION=1}、\code{ERROR\_PREDICTION=0} とした。
現行の標準最大エポック数は 128 である。
検証損失が 12 エポック改善しなければ学習を停止する。
ただし、検証損失が一時的に悪化してから再び改善する場合を考慮し、早期終了は 32 エポック目までは有効にしない。
検証損失が最小になった checkpoint は学習中に保存し、最終的な validation/test 評価では、その best validation checkpoint を読み戻して使う。

\subsection{出力}

再構成出力は、標準化された 6 つのターゲット座標である。
\[
  \hat{\mathbf{y}} =
  (\widehat{\log_{10}E},\hat{x}_c,\hat{y}_c,\hat{n}_x,\hat{n}_y,\hat{n}_z).
\]
エネルギー出力層は、共有事象表現から 1 次元を出力する線形層である。
コア出力層と方向出力層は、同じ共有事象表現を入力とする 2 層の MLP である。
方向は 3 成分のベクトルとして出力する。
角度誤差を計算するときは、予測ベクトルと真のベクトルを内部で正規化してから角距離を求める。

品質出力層は 1 つのロジットを出力する。
ロジットとは、用語節で定義した通り、シグモイド関数に入れる前の実数値である。
シグモイド関数を適用した値を、0 から 1 までの事象品質スコアとして解釈する。
この品質スコアは、再構成結果を物理解析に使うときの事象選択に用いる。

\subsection{物理再構成損失}

物理再構成損失は、標準化された予測値と教師ラベルを物理単位に戻してから計算する。
標準の重みは
\[
  w_E=1.2,\quad w_c=1.0,\quad w_\theta=1.4
\]
であり、損失は
\[
  L_{\mathrm{phys}} =
  w_E L_E + w_c L_c + w_\theta L_\theta
\]
である。

エネルギー項は、$\log_{10}E$ の差に対する SmoothL1 損失である。
SmoothL1 損失は、用語節で定義した通り、誤差が小さい領域では二乗誤差に近く、誤差が大きい領域では絶対値誤差に近い。
外れ値の影響を抑えながら、中心付近の精度を上げるために用いる。
\[
  L_E=\mathrm{SmoothL1}(\widehat{\log_{10}E}-\log_{10}E;\beta=0.05).
\]

コア項は、水平方向のコア誤差を $0.05\,\mathrm{km}$ で規格化した二乗誤差である。
\[
  L_c=\left\langle
  \frac{(\hat{x}_c-x_c)^2+(\hat{y}_c-y_c)^2}{(0.05\,\mathrm{km})^2}
  \right\rangle.
\]

方向項は、予測方向と真の方向の角距離 $\alpha$ を $1.0^\circ$ で規格化し、SmoothL1 損失を適用する。
\[
  L_\theta=\mathrm{SmoothL1}\left(\frac{\alpha}{1.0^\circ};\beta=1\right).
\]
この規格化により、角度誤差が損失全体の中で過小評価されることを防ぐ。

\subsection{エネルギーバイアス抑制項}

低エネルギー側で平均的に $\widehat{\log_{10}E}-\log_{10}E$ が正にずれることを抑えるため、
次の学習条件では、true energy bin ごとの平均残差に対するペナルティを加える。
これは MC 教師ラベルを使う学習時の制約であり、実データに対する後処理補正ではない。
残差を
\[
  \delta_i=\widehat{\log_{10}E_i}-\log_{10}E_i
\]
とし、true $\log_{10}E$ の幅 $0.1$ の bin を $B_k$ とすると、
\[
  L_{\mathrm{bias}}=
  \left\langle
  \left(
  \frac{1}{|B_k|}\sum_{i\in B_k}\delta_i
  \right)^2
  \right\rangle_k
\]
を用いる。
さらに、同じ true energy bin 内で陽子と鉄の平均残差が逆向きにずれて相殺されることを防ぐため、
\[
  L_{\mathrm{p/Fe}}=
  \left\langle
  \left(
  \langle\delta\rangle_{B_k,\mathrm{proton}}
  -
  \langle\delta\rangle_{B_k,\mathrm{iron}}
  \right)^2
  \right\rangle_k
\]
を加える。
標準投入スクリプトでは、再構成を含む学習に対して
\[
  \lambda_{\mathrm{bias}}=1.0,\qquad
  \lambda_{\mathrm{p/Fe}}=1.0
\]
を既定値とする。

\subsection{品質損失}

品質教師値は、現在の予測から計算した残差を用いて作る。
ただし、この教師値は計算グラフから切り離す。
したがって、品質出力は再構成出力を直接改善するためではなく、「この事象の再構成がどの程度信用できるか」を学習するために用いる。
これは陽子/鉄のような二値ラベルではなく、再構成残差から作る soft target である。
\[
  q_{\mathrm{target}}=
  \exp\left[-\frac{1}{3}\left(
  \frac{|E_{\mathrm{rec}}/E_{\mathrm{true}}-1|}{0.10}
  +\frac{\Delta r_c}{0.05\,\mathrm{km}}
  +\frac{\alpha}{1.0^\circ}
  \right)\right].
\]
品質出力層は、ロジット付き二値交差エントロピーで学習する。
\[
  L_q=\mathrm{BCEWithLogits}(q_{\mathrm{logit}},q_{\mathrm{target}}),
  \qquad w_q=0.2.
\]
標準設定での再構成の全損失は
\[
  L_{\mathrm{total}}=
  L_{\mathrm{phys}}
  +\lambda_{\mathrm{bias}}L_{\mathrm{bias}}
  +\lambda_{\mathrm{p/Fe}}L_{\mathrm{p/Fe}}
  +0.2\,L_q
\]
である。
品質スコアは、解析に使う事象を選ぶための 1 つの総合指標として用いる。
例えば、品質スコア上位 $95\%$、$90\%$、$80\%$ を残した場合の精度と残存率を評価する。

\subsection{予想誤差出力と負の対数尤度の扱い}

実装には、事象ごとの予想誤差を出力する head と、ガウス分布の負の対数尤度を再構成損失に加える \code{physics-nll} モードがある。
この出力では、3 つの raw 値を softplus 関数で正の値に変換し、
\[
  (\sigma_E,\sigma_\theta,\sigma_c)
  =\mathrm{softplus}(r)\odot(0.10,\ 0.8^\circ,\ 0.04\,\mathrm{km})
\]
として、相対エネルギー誤差、角度誤差、コア位置誤差の予想値を得る。

負の対数尤度は、予測誤差 $\sigma$ を小さくしすぎると $r^2/\sigma^2$ が大きくなり、逆に大きくしすぎると $\log\sigma$ が大きくなる、という性質を使う。
固定された残差 $r$ に対して
\[
  \frac{1}{2}\frac{r^2}{\sigma^2}+\log\sigma
\]
を最小化すると、概念的には $\sigma^2\simeq r^2$ が好まれる。
しかし、2026 年 5 月 22--23 日の比較では、この方法を標準設定に入れる根拠は得られなかった。
そのため、現時点では \code{ERROR\_PREDICTION=0}、\code{NLL\_WEIGHT=0.0} を標準とする。
予想誤差 head を再び試す場合は、まず再構成損失に混ぜず、実際の誤差を教師値とする補助損失として別に評価する必要がある。

\subsection{品質出力と予想誤差出力の比較}

表~\ref{tab:quality-vs-nll} に、同じ HDF5 グラフ入力 \code{waveform\_gnn\_stream\_20260520\_123802} を用いた 2 つの再構成 run の比較を示す。
この比較は、現時点で \code{physics-nll} よりも品質出力のみを用いる学習を標準に戻す理由である。

\begin{table}[htbp]
  \centering
  \caption{品質出力のみの run と、予想誤差 head および NLL を加えた run のテスト性能。}
  \label{tab:quality-vs-nll}
  \begin{tabular}{lcc}
    \toprule
    指標 & 品質出力のみ & 予想誤差+NLL \\
    \midrule
    run 名 & \code{20260522\_054351} & \code{20260522\_182001} \\
    最大エポック数 & 48 & 96 \\
    損失モード & \code{physics} & \code{physics-nll} \\
    予想誤差 head & なし & あり \\
    median $|\Delta\log_{10}E|$ & 0.0588 & 0.0601 \\
    RMSE $\log_{10}E$ & 0.1154 & 0.1158 \\
    相対エネルギー中央 68\% 幅 & 0.4297 & 0.4552 \\
    相対エネルギー 68\% & 0.2096 & 0.2200 \\
    角距離 68\% & $1.227^\circ$ & $1.281^\circ$ \\
    コア位置 68\% & 47.5 m & 50.7 m \\
    \bottomrule
  \end{tabular}
\end{table}

品質出力のみの run では、テスト全体の角度分解能とコア位置分解能が、予想誤差+NLL の run より良い。
エネルギー指標でも、品質出力のみの run が同等または良い。
品質カット後の性能も、図~\ref{fig:quality_vs_nll_cuts} に示す通り同じ傾向である。
したがって、現状の実装とデータでは、予想誤差 head を NLL として再構成損失に混ぜる設定は標準にしない。
標準設定は、品質スコアを別 head として学習し、その品質で事象選択する形である。

予想誤差+NLL の run では、検証損失が 42 エポック付近で最小になった後、最終エポックまで悪化した。
この時点で validation loss は train loss から離れており、標準設定として採用しない。
一方、品質出力のみの run では、48 エポック目まで検証損失の悪化は顕著ではなかった。
この違いも、標準設定を品質出力のみへ戻す理由である。

\begin{figure}[p]
  \centering
  \begin{minipage}{0.49\linewidth}
    \centering
    \includegraphics[width=\linewidth]{fig/results/quality_only_learning_curve.pdf}
    {\small 品質出力のみ}
  \end{minipage}
  \hfill
  \begin{minipage}{0.49\linewidth}
    \centering
    \includegraphics[width=\linewidth]{fig/results/physics_nll_learning_curve.pdf}
    {\small 予想誤差+NLL}
  \end{minipage}
  \caption{再構成 run の学習曲線の比較。予想誤差+NLL では、検証損失の悪化が見える。}
  \label{fig:quality_vs_nll_learning}
\end{figure}

\begin{figure}[p]
  \centering
  \begin{minipage}{0.49\linewidth}
    \centering
    \includegraphics[width=\linewidth]{fig/results/quality_only_test_quality_cut_performance.pdf}
    {\small 品質出力のみ}
  \end{minipage}
  \hfill
  \begin{minipage}{0.49\linewidth}
    \centering
    \includegraphics[width=\linewidth]{fig/results/physics_nll_test_quality_cut_performance.pdf}
    {\small 予想誤差+NLL}
  \end{minipage}
  \caption{品質カットによるテスト性能の比較。現状では、品質出力のみの run の方が、同じ残存率で良い分解能を与える。}
  \label{fig:quality_vs_nll_cuts}
\end{figure}

\begin{figure}[p]
  \centering
  \includegraphics[width=0.95\linewidth]{fig/results/physics_nll_test_predicted_vs_actual_error.pdf}
  \caption{予想誤差+NLL run における予想誤差と実際の誤差の対応。エネルギー誤差の順位相関は弱く、予想誤差 head を標準の事象選択指標として使うには不十分である。}
  \label{fig:predicted_vs_actual_error}
\end{figure}

\section{データセット規模と現在の再構成結果}

表~\ref{tab:dataset-reco-results} に、再構成タスクで得られている主な結果を示す。
\code{waveform\_gnn\_stream\_20260520\_123802} は、品質出力のみを用いた旧基準 run である。
\code{flat2000} と \code{flat20000} は、energy-flat HDF5 から作った小規模・中規模確認用データセットである。
\code{flat50000} の完了済み run は
\code{flat50000\_reco\_a100\_128epoch\_20260601\_003958} である。
この run は Slurm sbatch で \code{PARTITION=a100\_long-al9}、\code{GRAPH\_INPUT\_ORIGINAL=/dicos\_ui\_home/ikomae/work/gnn/graphs/flat50000} として投入され、run directory の \code{train.env} と metrics JSON では \code{source-stratified}、validation 5\%、test 10\%、best validation epoch 44 が確認できる。
ただし、この表の \code{flat50000} 結果は、現行 \code{flat50000\_wfmask\_target6\_20260603\_102418} HDF5 を使った結果ではない。
波形 mask schema と 6 成分 target schema を用いた再学習結果は、この表にはまだ含めない。

\begin{table}[p]
  \centering
  \scriptsize
  \caption{再構成 run のデータセット規模とテスト性能。}
  \label{tab:dataset-reco-results}
  \begin{tabular}{lrrrrrrr}
    \toprule
    graph/run & train & val & test & median $|\Delta\log_{10}E|$ & RMSE $\log_{10}E$ & core 68\% & angle 68\% \\
    \midrule
    \code{waveform\_gnn\_stream} &
    2,005,027 & 290,966 & 571,019 & 0.0588 & 0.1154 & 47.5 m & $1.227^\circ$ \\
    \code{flat2000} &
    104,076 & 6,123 & 12,267 & 0.0811 & 0.1626 & 73.5 m & $1.866^\circ$ \\
    \code{flat20000} &
    993,449 & 58,486 & 117,085 & 0.0628 & 0.1384 & 52.0 m & $1.405^\circ$ \\
    \code{flat50000} A100 &
    2,434,268 & 143,192 & 286,615 & 0.0610 & 0.1353 & 45.6 m & $1.403^\circ$ \\
    \bottomrule
  \end{tabular}
\end{table}

\code{flat2000} は各エネルギー・粒子 bin の事象数が少なく、精度評価用としては不足している。
\code{flat50000} では、\code{flat20000} よりコア位置 68\% が改善した。
一方、test 全体の角距離 68\% は $1.403^\circ$ であり、precision report の全事象 target である $1^\circ$ には届いていない。
同じ precision report では、品質スコアによる事象選択を使うと、残存率 50\% 以降で energy Gaussian sigma、角距離 68\%、コア 68\% の target を満たす。
run の最終ログでは \code{precision\_status=PASS} と記録されているが、これは品質 cut 候補を含む判定であり、test 全体の角度 target 達成を意味しない。

\begin{table}[htbp]
  \centering
  \small
  \caption{\code{flat50000\_reco\_a100\_128epoch\_20260601\_003958} の test 品質 cut 性能。}
  \label{tab:flat50000-quality-cuts}
  \begin{tabular}{rrrrr}
    \toprule
    残存率 & $q_{\mathrm{cut}}$ & energy sigma & angle 68\% & core 68\% \\
    \midrule
    100\% & 0.0086 & 0.1834 & $1.403^\circ$ & 45.6 m \\
    80\% & 0.2297 & 0.1654 & $1.177^\circ$ & 38.7 m \\
    60\% & 0.2932 & 0.1480 & $1.015^\circ$ & 34.0 m \\
    50\% & 0.3266 & 0.1391 & $0.939^\circ$ & 31.5 m \\
    30\% & 0.4032 & 0.1194 & $0.786^\circ$ & 26.1 m \\
    10\% & 0.4983 & 0.0962 & $0.602^\circ$ & 19.4 m \\
    5\% & 0.5291 & 0.0864 & $0.528^\circ$ & 17.3 m \\
    \bottomrule
  \end{tabular}
\end{table}

エネルギー bin 別では、低エネルギー側、特に $\log_{10}E=15.95$--16.35 付近で energy bias と energy sigma が大きい。
角度 68\% は多くの bin で $1^\circ$ を超え、$\log_{10}E=18.45$--18.55 付近では test 事象数が 1021 と少なく、コア 68\% も 92.7 m と悪化している。
これらは diagnostics summary と precision report の bin 別 FAIL として確認できるため、test 全体の代表値だけで性能を判断しない。

\begin{figure}[p]
  \centering
  \begin{minipage}{0.49\linewidth}
    \centering
    \includegraphics[width=\linewidth]{fig/results/flat50000_reco_a100_learning_curve.pdf}
    {\small 学習曲線}
  \end{minipage}
  \hfill
  \begin{minipage}{0.49\linewidth}
    \centering
    \includegraphics[width=\linewidth]{fig/results/flat50000_reco_a100_test_quality_cut_performance.pdf}
    {\small 品質 cut 性能}
  \end{minipage}
  \caption{\code{flat50000} A100 再構成 run の学習曲線と test 品質 cut 性能。best validation checkpoint を読み戻して評価した。}
  \label{fig:flat50000-reco-learning-quality}
\end{figure}

\begin{figure}[p]
  \centering
  \begin{minipage}{0.49\linewidth}
    \centering
    \includegraphics[width=\linewidth]{fig/results/flat50000_reco_a100_test_energy_resolution_by_true_energy.pdf}
    {\small エネルギー分解能}
  \end{minipage}
  \hfill
  \begin{minipage}{0.49\linewidth}
    \centering
    \includegraphics[width=\linewidth]{fig/results/flat50000_reco_a100_test_angular_core_resolution_by_true_energy.pdf}
    {\small 角度・コア分解能}
  \end{minipage}
  \caption{\code{flat50000} A100 再構成 run の test エネルギー依存性。}
  \label{fig:flat50000-reco-energy-dependence}
\end{figure}

\section{質量分類のみのモデル}

質量分類のみの学習には
\[
\code{scripts/submit\_server\_mass\_only\_training.sh}
\]
を用いる。
このスクリプトは、同じ学習ラッパーを呼び出し、\code{TRAINING\_TASK=mass}、\code{MASS\_CLASSIFICATION=1}、\code{QUALITY\_PREDICTION=0}、\code{ERROR\_PREDICTION=0} を設定する。
現行の標準最大エポック数は 128 である。
検証損失が 12 エポック改善しなければ停止し、32 エポック目までは早期終了しない。
再構成と同じく、最終評価では best validation checkpoint を読み戻す。
モデル定義上は再構成出力層も存在するが、質量分類のみの学習では再構成損失を使わない。

\subsection{質量分類ヘッド}

現行の質量分類では、拡張分類構造（enhanced classification architecture）を用いる。
これは、最終的な共有事象表現だけでなく、初期ノード表現や波形の直接要約も分類に使う構造である。
入力は次の 4 種類のベクトルを連結したものである。

\begin{itemize}
  \item 最終グラフ読み出し。次元は 1152 である。
  \item ノードエンコーダ直後の平均プーリングと最大プーリング。次元は $2H=384$ である。
  \item グラフの規模を表す文脈量 $(\log(1+N_v),\log(1+N_e),N_e/N_v)$。次元は 3 である。
  \item 波形の直接読み出し。波形埋め込みの平均と最大値、および波形形状要約の平均と最大値を連結する。次元は $2D_w+2\times20=168$ である。
\end{itemize}

したがって、分類ヘッドの入力次元は
\[
  1152+384+3+168=1707
\]
である。
分類ヘッドは
\[
\mathrm{LayerNorm}(1707)
\rightarrow384
\rightarrow192
\rightarrow\mathrm{LayerNorm}
\rightarrow192
\rightarrow1
\]
である。
各中間層には SiLU とドロップアウトを用いる。
出力は 1 つのロジット $z$ である。
シグモイド関数を適用した
\[
  p(\mathrm{Fe})=\sigma(z)
\]
を、鉄である確率として扱う。
ラベルの定義は、陽子が 0、鉄が 1 である。

\subsection{質量分類損失}

質量分類スクリプトの既定の分類損失は、ロジット付き二値交差エントロピーである。
\[
  L_{\mathrm{BCE}}=
  -y\log\sigma(z)-(1-y)\log(1-\sigma(z)).
\]
現行設定では、正例である鉄に対する追加のクラス重みは使わない。
すなわち、\code{MASS\_POS\_WEIGHT\_MODE=none} である。

実装には focal loss もある。
事象 $i$ のロジットを $z_i$、ラベルを $y_i\in\{0,1\}$、$p_i=\sigma(z_i)$ とし、
\[
  p_{t,i}=
  \begin{cases}
    p_i, & y_i=1,\\
    1-p_i, & y_i=0
  \end{cases}
\]
と定義する。
このとき focal loss は
\[
  L_{\mathrm{focal}}=
  \left\langle
  (1-p_{t,i})^\gamma\,
  \mathrm{BCEWithLogits}(z_i,y_i)
  \right\rangle_i
\]
である。
2026 年 6 月 1 日に完了した \code{flat50000} の質量分類 run では、既定の BCE ではなく、明示的に \code{MASS\_LOSS\_MODE=focal}、\code{MASS\_FOCAL\_GAMMA=2.0} を指定した。

さらに、同じバッチに陽子と鉄が含まれる場合にはランキング損失を加える。
鉄事象のロジットを $z^+$、陽子事象のロジットを $z^-$ とすると、
\[
  L_{\mathrm{rank}}=
  \left\langle
  \mathrm{softplus}\left(1.0-(z^+-z^-)\right)
  \right\rangle
\]
である。
この項は、鉄のロジットが陽子のロジットより大きくなるように促す。
バッチ内に片方の粒子種しか含まれない場合、このランキング項はそのバッチでは計算しない。

submitter 既定の BCE 設定での質量分類の全損失は
\[
  L_{\mathrm{mass}} = L_{\mathrm{BCE}} + 0.5\,L_{\mathrm{rank}}
\]
である。
今回の \code{flat50000} focal run では
\[
  L_{\mathrm{mass}} = L_{\mathrm{focal}} + 0.5\,L_{\mathrm{rank}}
\]
を用いた。
現行スクリプトでは、質量分類の過学習を抑えるために \code{DROPOUT=0.12}、\code{WEIGHT\_DECAY=5e-4}、\code{LR\_SCHEDULER=cosine} を標準にしている。
学習中の自然な分類境界は $p(\mathrm{Fe})=0.5$ である。
診断では、検証データを用いて分類しきい値を後から調整することもできる。
しかし、モデル自体は、ロジット 0 が陽子と鉄の境界になるように学習させる。

\subsection{現在の質量分類結果}

表~\ref{tab:mass-results} に、質量分類のみの主な結果を示す。
最新 run は \code{flat50000\_mass\_focal\_v100\_128epoch\_20260601\_003958} である。
この run は Slurm sbatch で \code{PARTITION=v100-al9\_long}、\code{GRAPH\_INPUT\_ORIGINAL=/dicos\_ui\_home/ikomae/work/gnn/graphs/flat50000} として投入され、run directory の \code{train.env} では \code{MASS\_LOSS\_MODE=focal}、\code{MASS\_FOCAL\_GAMMA=2.0}、\code{MASS\_RANKING\_WEIGHT=0.5}、\code{source-stratified}、validation 5\%、test 10\% が確認できる。
best validation epoch は 15 である。

\begin{table}[htbp]
  \centering
  \scriptsize
  \caption{質量分類のみ run の test 性能。しきい値調整は validation で選んだ値を test に適用する。}
  \label{tab:mass-results}
  \begin{tabular}{llrrrrr}
    \toprule
    run & 損失 & test & accuracy & balanced acc. & AUC & tuned balanced acc. \\
    \midrule
    \code{server\_mass\_waveform\_direct\_48epoch\_20260522\_143042} &
    BCE & -- & 0.800 & 0.801 & 0.887 & -- \\
    \code{server\_mass\_b6000\_128epoch\_20260527\_042231} &
    BCE & 117,082 & 0.790 & 0.791 & 0.875 & -- \\
    \code{flat50000\_mass\_focal\_v100\_128epoch\_20260601\_003958} &
    focal & 286,618 & 0.802 & 0.802 & 0.889 & 0.805 \\
    \bottomrule
  \end{tabular}
\end{table}

\code{flat50000} focal run の固定しきい値 $p(\mathrm{Fe})=0.5$ での test confusion counts は、true proton 146,818 のうち true negative 117,311、false positive 29,507、true iron 139,800 のうち true positive 112,617、false negative 27,183 である。
validation で選んだ tuned threshold は 0.4601 であり、test balanced accuracy は 0.802 から 0.805 に上がる。
ただし、改善幅は小さいため、現状の制限は単純なしきい値ではなく score separation にある。
test score median は陽子 0.1999、鉄 0.6703 であり、AUC は 0.889 である。

過去の \code{flat50000\_reco\_mass\_v100\_20260529\_013303} は、\code{MASS\_LOSS\_WEIGHT=0.05}、\code{MASS\_LOSS\_MODE=bce}、\code{MASS\_RANKING\_WEIGHT=0.5}、\code{CORE\_SCALE\_KM=0.12} であった。
best validation epoch 24 では、validation loss の内訳は
\[
  L_{\mathrm{reco}}=1.754,\qquad
  0.2L_q=0.118,\qquad
  0.05L_{\mathrm{mass}}=0.034
\]
であり、mass 項は total loss の約 1.8\% に留まっていた。
この run の test mass balanced accuracy は 0.792、tuned threshold 後で 0.798 であり、同時期の mass-only BCE run の 0.805、0.807 より低い。
したがって、今後の reco+mass ベースでは、mass loss を品質損失と同程度の寄与に近づけるため、\code{MASS\_LOSS\_WEIGHT=0.15} を次候補とする。
この値は過去 run の loss scale から決めた次の投入条件であり、\code{CORE\_SCALE\_KM=0.05} と組み合わせた full run ではまだ検証していない。

\begin{figure}[p]
  \centering
  \begin{minipage}{0.49\linewidth}
    \centering
    \includegraphics[width=\linewidth]{fig/results/flat50000_mass_focal_v100_learning_curve.pdf}
    {\small 学習曲線}
  \end{minipage}
  \hfill
  \begin{minipage}{0.49\linewidth}
    \centering
    \includegraphics[width=\linewidth]{fig/results/flat50000_mass_focal_v100_test_mass_roc.pdf}
    {\small test ROC}
  \end{minipage}
  \caption{\code{flat50000} V100 mass focal run の学習曲線と test ROC。}
  \label{fig:flat50000-mass-learning-roc}
\end{figure}

\begin{figure}[p]
  \centering
  \begin{minipage}{0.32\linewidth}
    \centering
    \includegraphics[width=\linewidth]{fig/results/flat50000_mass_focal_v100_test_mass_score_distribution.pdf}
    {\small score 分布}
  \end{minipage}
  \hfill
  \begin{minipage}{0.32\linewidth}
    \centering
    \includegraphics[width=\linewidth]{fig/results/flat50000_mass_focal_v100_test_mass_confusion_matrix.pdf}
    {\small confusion matrix}
  \end{minipage}
  \hfill
  \begin{minipage}{0.32\linewidth}
    \centering
    \includegraphics[width=\linewidth]{fig/results/flat50000_mass_focal_v100_test_mass_accuracy_by_true_energy.pdf}
    {\small エネルギー bin 別 accuracy}
  \end{minipage}
  \caption{\code{flat50000} V100 mass focal run の test score 分布、confusion matrix、エネルギー依存性。}
  \label{fig:flat50000-mass-score-energy}
\end{figure}

\section{再現に必要な設定}

表~\ref{tab:quality-vs-nll} の比較を再現するには、比較用 HDF5 グラフ片を用いる。
今後のベースは、現行 HDF5 \code{flat50000\_wfmask\_target6\_20260603\_102418} を用い、再構成と質量分類を同時に学習する reco+mass である。
標準入口は次である。
\begin{verbatim}
GRAPH_INPUT=/dicos_ui_home/ikomae/work/gnn/graphs/flat50000_wfmask_target6_20260603_102418 \
PARTITION=v100-al9_long \
RUN_NAME=flat50000_wfmask_target6_reco_mass_v100_128epoch_$(date +%Y%m%d_%H%M%S) \
scripts/submit_server_reco_mass_training.sh
\end{verbatim}
現在の reco+mass ベース設定は、過去 run の loss scale から \code{MASS\_LOSS\_WEIGHT=0.15}、\code{MASS\_LOSS\_MODE=bce}、\code{MASS\_RANKING\_WEIGHT=0.5} とする。
再構成側は現行標準に合わせ、\code{CORE\_SCALE\_KM=0.05}、\code{QUALITY\_CORE\_SCALE\_KM=0.05}、\code{QUALITY\_PREDICTION=1}、\code{ERROR\_PREDICTION=0} とする。
低エネルギー側の energy bias と核種ごとの差を抑えるため、\code{ENERGY\_BIAS\_WEIGHT=1.0}、\code{ENERGY\_PARTICLE\_BIAS\_WEIGHT=1.0} も用いる。
この reco+mass 条件は次に検証すべきベースであり、2026 年 6 月 1 日時点で完了済みの最新 run は、reco-only と mass-only を別々に走らせた結果である。

2026 年 6 月 1 日に完了した旧 schema \code{flat50000} 再構成結果を再現するには、次のグラフ入力と再構成スクリプトを用いる。
\begin{verbatim}
GRAPH_INPUT=/dicos_ui_home/ikomae/work/gnn/graphs/flat50000 \
RUN_NAME=flat50000_reco_a100_128epoch_$(date +%Y%m%d_%H%M%S) \
PARTITION=a100_long-al9 \
ALLOW_A100=1 \
scripts/submit_server_waveform_full_training.sh
\end{verbatim}
この run の主な設定は次の通りである。
\[
\begin{array}{ll}
\code{MODEL\_ARCHITECTURE=physics}, & \code{HIDDEN\_DIM=192},\\
\code{LAYERS=5}, & \code{WAVEFORM\_ENCODER=cnn-gru},\\
\code{LOSS\_MODE=physics}, & \code{TRAIN\_EPOCHS=128},\\
\code{QUALITY\_PREDICTION=1}, & \code{ERROR\_PREDICTION=0},\\
\code{BATCH\_SIZE=256}, & \code{SPLIT\_MODE=source-stratified},\\
\code{VAL\_FRACTION=0.05}, & \code{TEST\_FRACTION=0.10},\\
\code{EARLY\_STOPPING\_PATIENCE=12}, & \code{EARLY\_STOPPING\_MIN\_EPOCHS=32}.
\end{array}
\]

質量分類のみの学習を再現するには、質量分類スクリプトの標準設定を使う。
submitter 既定の主な設定は次の通りである。
\[
\begin{array}{ll}
\code{TRAINING\_TASK=mass}, & \code{MASS\_CLASSIFICATION=1},\\
\code{CLASSIFICATION\_ARCH=enhanced}, & \code{MASS\_POS\_WEIGHT\_MODE=none},\\
\code{MASS\_LOSS\_MODE=bce}, & \code{MASS\_RANKING\_WEIGHT=0.5},\\
\code{MASS\_RANKING\_MARGIN=1.0}, & \code{TRAIN\_EPOCHS=128},\\
\code{DROPOUT=0.12}, & \code{WEIGHT\_DECAY=5e-4},\\
\code{LR\_SCHEDULER=cosine}, & \code{EARLY\_STOPPING\_PATIENCE=12}.
\end{array}
\]
2026 年 6 月 1 日に完了した旧 schema \code{flat50000} mass focal 結果を再現する場合は、submitter 既定の \code{MASS\_LOSS\_MODE=bce} ではなく、次のように focal loss を明示する。
\begin{verbatim}
GRAPH_INPUT=/dicos_ui_home/ikomae/work/gnn/graphs/flat50000 \
PARTITION=v100-al9_long \
RUN_NAME=flat50000_mass_focal_v100_128epoch_$(date +%Y%m%d_%H%M%S) \
MASS_LOSS_MODE=focal \
MASS_FOCAL_GAMMA=2.0 \
MASS_RANKING_WEIGHT=0.5 \
scripts/submit_server_mass_only_training.sh
\end{verbatim}

旧基準データセットを現行コードで切り分ける比較では、次の 2 本を使う。
\begin{verbatim}
RUN_NAME=oldgraph_reco_b6000_128e \
GRAPH_INPUT=/dicos_ui_home/ikomae/work/gnn/graphs/waveform_gnn_stream_20260520_123802 \
scripts/submit_server_waveform_full_training.sh

RUN_NAME=oldgraph_oldsplit_reco_b6000_128e \
GRAPH_INPUT=/dicos_ui_home/ikomae/work/gnn/graphs/waveform_gnn_stream_20260520_123802 \
VAL_FRACTION=0.10 \
TEST_FRACTION=0.20 \
scripts/submit_server_waveform_full_training.sh
\end{verbatim}

チェックポイントには、モデル設定、特徴量スケーラ、ターゲットスケーラ、データ分割のインデックス、学習履歴、評価指標、診断図の出力先を保存する。
また、診断ディレクトリには予測キャッシュを保存する。
そのため、テストデータの図を作り直すときに DST を再度読む必要はない。

\end{document}
